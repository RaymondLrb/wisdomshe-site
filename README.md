# GitHub Pages 部署

1. 将仓库根目录作为 Pages 源。
2. 保留 `index.html`、`404.html`、`assets/`、`CNAME`、`.nojekyll`。
3. 在仓库设置里启用 GitHub Pages，选择 `Deploy from a branch`，分支 `main`，目录 `/ (root)`。
4. 推送后等待几分钟，打开你的 Pages 地址即可。
